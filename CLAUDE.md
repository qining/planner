# CLAUDE.md

**开发本项目前请先完整阅读 [AGENTS.md](./AGENTS.md)。**

那里记录了这个 repo 的铁律、实测有效的开发与验证流程、踩过的坑，
以及 LLM agent 审查/验证的方法论。不读会返工。

三条最容易踩的：

1. `#calib` 校准输出必须逐字节不变（基线 md5 `1ac26921871db50ef1c055674c10e6e7`）——
   户型几何经 30 轮独立审查才对齐，新增视觉元素一律不能进 calib 视图。
2. 交互功能**必须**用 `work/t_walledit.html` 那套自动化交互测试台验证（当前 46 条断言），
   截图冒烟测不出交互 bug。
3. 改完 `planner.html` 后：语法检查 → 重新生成测试页 → 跑测试 → 校准回归 → 视觉目检。

4. 性能相关改动看 AGENTS.md §11：几何合并/按需渲染/增量同步已经做了，
   测量时注意 `--virtual-time-budget` 会冻结页面内计时器（必须用进程墙钟 + 完成标记）。

回复用中文；提到文件/页面给完整 `file://` URL。
